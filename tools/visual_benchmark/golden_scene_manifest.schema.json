{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/mosca200/rc-simulation-engine/schemas/golden_scene_manifest.json",
  "title": "Golden Scene Manifest",
  "description": "Machine-readable contract for reproducible visual benchmarks of the RC Simulation Engine renderer. v1 supports only runtime-configurable parameters. Future capabilities are documented separately.",
  "type": "object",
  "required": [
    "schema_version",
    "scene_id",
    "description",
    "renderer",
    "scenery",
    "camera",
    "resolution",
    "exposure_ev",
    "aircraft",
    "warmup",
    "capture",
    "tags"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Semantic version of this schema (e.g., '1.0.0'). Used for forward compatibility."
    },
    "scene_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "minLength": 3,
      "maxLength": 128,
      "description": "Unique identifier for this scene. Format: <category>_<scenario>_<variant>. Example: 'aircraft_acro_static_front'."
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "maxLength": 1000,
      "description": "Human-readable description of what this scene tests or demonstrates."
    },
    "renderer": {
      "type": "object",
      "required": ["version"],
      "additionalProperties": false,
      "properties": {
        "version": {
          "type": "string",
          "enum": ["v1", "v2"],
          "description": "Renderer backend version. 'v1' = legacy, 'v2' = physical atmosphere/IBL. Maps to --renderer CLI flag."
        },
        "terrain_debug": {
          "type": "string",
          "enum": ["final", "wireframe", "normals", "uv", "bounds"],
          "default": "final",
          "description": "Terrain debug visualization mode. Maps to --terrain-debug CLI flag."
        },
        "vegetation_debug": {
          "type": "string",
          "enum": ["final", "wireframe", "bounds", "lod"],
          "default": "final",
          "description": "Vegetation debug visualization mode. Maps to --vegetation-debug CLI flag."
        }
      },
      "description": "Renderer configuration. Only runtime-configurable parameters."
    },
    "scenery": {
      "type": "object",
      "required": ["preset"],
      "additionalProperties": false,
      "properties": {
        "preset": {
          "type": "string",
          "enum": ["none", "flying-field"],
          "description": "Scenery preset. Maps to --scenery CLI flag. 'none' = empty, 'flying-field' = RC airfield with runway, markings, fence, windsock."
        }
      },
      "description": "Scenery environment configuration. Only runtime-configurable parameters."
    },
    "camera": {
      "type": "object",
      "required": ["mode", "vertical_fov_deg"],
      "additionalProperties": false,
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["pilot", "chase"],
          "description": "Camera mode. Maps to --camera CLI flag."
        },
        "vertical_fov_deg": {
          "type": "number",
          "minimum": 10,
          "maximum": 120,
          "description": "Vertical field of view in degrees. Maps to --vertical-fov-deg CLI flag. Typical: 55-70."
        },
        "pilot_position_render_m": {
          "type": "array",
          "items": {"type": "number"},
          "minItems": 3,
          "maxItems": 3,
          "description": "Pilot camera position in render-body frame [x, y, z] in meters. Required when mode='pilot'. Maps to --pilot-position-render-m CLI flag."
        },
        "chase_distance_behind_m": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 50,
          "description": "Chase camera distance behind aircraft in meters. Required when mode='chase'. Maps to --chase-distance-m CLI flag."
        },
        "chase_height_above_m": {
          "type": "number",
          "minimum": -10,
          "maximum": 50,
          "description": "Chase camera height above aircraft in meters. Required when mode='chase'. Maps to --chase-height-m CLI flag."
        }
      },
      "allOf": [
        {
          "if": {"properties": {"mode": {"const": "pilot"}}},
          "then": {"required": ["pilot_position_render_m"]}
        },
        {
          "if": {"properties": {"mode": {"const": "chase"}}},
          "then": {"required": ["chase_distance_behind_m", "chase_height_above_m"]}
        }
      ],
      "description": "Camera configuration. Mode-specific parameters are required based on the mode value."
    },
    "resolution": {
      "type": "object",
      "required": ["width", "height"],
      "additionalProperties": false,
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 320,
          "maximum": 7680,
          "description": "Image width in pixels."
        },
        "height": {
          "type": "integer",
          "minimum": 240,
          "maximum": 4320,
          "description": "Image height in pixels."
        }
      },
      "description": "Output image resolution."
    },
    "exposure_ev": {
      "type": "number",
      "minimum": -8,
      "maximum": 8,
      "description": "Manual exposure value in EV stops. Maps to --exposure-ev CLI flag. 0 = neutral, +1 = 2x brighter, -1 = 2x darker. Runtime range: [-8, 8]."
    },
    "aircraft": {
      "type": "object",
      "required": ["model"],
      "additionalProperties": false,
      "properties": {
        "model": {
          "type": "string",
          "pattern": "^models/[a-z0-9_]+/model\\.json$",
          "description": "Path to aircraft model.json relative to repo root. Maps to --model CLI flag. Example: 'models/acro_electric_01/model.json'."
        },
        "throttle": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "default": 0.55,
          "description": "Engine throttle setting 0.0-1.0. Maps to --throttle CLI flag."
        },
        "start_on_ground": {
          "type": "boolean",
          "default": false,
          "description": "Whether aircraft starts on ground. Maps to --start-on-ground CLI flag."
        }
      },
      "description": "Aircraft configuration. Only runtime-configurable parameters."
    },
    "warmup": {
      "type": "integer",
      "minimum": 0,
      "maximum": 300,
      "default": 10,
      "description": "Number of frames to render before capture. Allows renderer to stabilize."
    },
    "capture": {
      "type": "object",
      "required": ["filename", "format"],
      "additionalProperties": false,
      "properties": {
        "filename": {
          "type": "string",
          "pattern": "^[a-z0-9_]+\\.(png|jpg|exr)$",
          "description": "Output filename. Must match format extension. Example: 'aircraft_acro_static_front_1920x1080.png'."
        },
        "format": {
          "type": "string",
          "enum": ["png", "jpg", "exr"],
          "description": "Image format. PNG = lossless, JPG = lossy, EXR = HDR."
        },
        "frame": {
          "type": "integer",
          "minimum": 0,
          "description": "Specific frame number to capture. If omitted, captures frame after warmup."
        },
        "quality": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100,
          "default": 95,
          "description": "JPEG quality 1-100. Only used when format=jpg."
        }
      },
      "description": "Capture configuration."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_-]*$"
      },
      "minItems": 1,
      "maxItems": 20,
      "uniqueItems": true,
      "description": "Tags for categorization and filtering. Example: ['aircraft', 'acro', 'static', 'front-view']."
    },
    "reference_hardware": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "gpu": {
          "type": "string",
          "description": "GPU model. Example: 'NVIDIA RTX 4090'."
        },
        "driver_version": {
          "type": "string",
          "description": "GPU driver version. Example: '546.33'."
        },
        "os": {
          "type": "string",
          "description": "Operating system. Example: 'Windows 11 23H2'."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes about reference hardware setup."
        }
      },
      "description": "Reference hardware for which this baseline was validated. Optional but recommended."
    },
    "reference_image": {
      "type": "string",
      "description": "Path to reference image for comparison. Optional. Example: 'baselines/rtx_4090_windows/aircraft_acro_static_front_1920x1080.png'."
    }
  }
}
